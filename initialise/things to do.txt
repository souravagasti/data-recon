Git commands

git add . To stage all
git add path/to/your_file.py 		to stage particular
git commit -m "Your meaningful commit message"
git push origin main
git diff --cached 		preview commit



DAB commands

databricks bundle validate
databricks bundle deploy --dry-run  		shows what's abt to change
databricks bundle deploy 		->as per databricks.yml
databricks auth login --profile <your-profile-name> 	to reauthenticate